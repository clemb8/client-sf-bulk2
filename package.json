{
  "name": "client-sf-bulk2",
  "version": "0.0.2",
  "description": "A client for the Salesforce bulk API V2",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build",
    "prepublishOnly": "npm run lint",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "generate-changelog": "github-changes -n ${npm_package_version} --use-commit-body -o clemb8 -r client-sf-bulk2",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "npm run generate-changelog && git add CHANGELOG.md",
    "postversion": "git push && git push --tags"
  },
  "author": "clemb8",
  "license": "MIT",
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "axios": "^0.27.2"
  }
}
